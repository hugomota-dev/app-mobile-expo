import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    backgroundColor: "#fff",
    flexDirection: "row",
    alignItems: "center",
    paddingLeft: 16,
    borderRadius: 10,
    borderColor: "#ccc",
    borderBottomWidth: 1,
    overflow: "hidden",
    padding: 12,
  },
  input: {
    fontSize: 18,
    paddingLeft: 12,
    width: "80%",
    height: 40,
    color: "#757575",
  },
});
